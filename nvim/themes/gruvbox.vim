" Most terminalds don't handle italics right so gruvbox disables italics for
" terminals by default. But if you're using gnome-terminal, add the following
" line before `colorscheme gruvbox` to enforce displaying italics 
let g:gruvbox_italic=1

colorscheme gruvbox

set termguicolors
